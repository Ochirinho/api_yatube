# 🧩 Yatube API Service

**Yatube API** — это REST API-сервис для социальной сети, где пользователи могут публиковать посты, комментировать их, объединяться в группы и подписываться друг на друга.

---

## 🚀 Возможности API

* 🔹 Регистрация и авторизация пользователей (через JWT)
* 📝 Создание, редактирование и удаление постов
* 💬 Комментирование постов
* 🏷️ Объединение постов в группы
* 🤝 Подписка на других пользователей
* 🔍 Поиск по подпискам
* 🔐 Разграничение прав доступа

---

## 🏗️ Технологии

* **Python 3.9+**
* **Django 3.2+**
* **Django REST Framework**
* **Djoser + JWT**
* **SQLite / PostgreSQL**

---

## ⚙️ Установка и запуск

### 1️⃣ Клонировать репозиторий:

```bash
git clone https://github.com/yourusername/yatube_api.git
cd yatube_api
```

### 2️⃣ Установить зависимости:

```bash
python -m venv venv
source venv/bin/activate   # для Linux/Mac
venv\Scripts\activate      # для Windows

pip install -r requirements.txt
```

### 3️⃣ Выполнить миграции:

```bash
python manage.py migrate
```

### 4️⃣ Создать суперпользователя:

```bash
python manage.py createsuperuser
```

### 5️⃣ Запустить сервер:

```bash
python manage.py runserver
```

---

## 🔑 Аутентификация

API использует **JWT-токены** через библиотеку **Djoser**.

Получение токена:

```
POST /api/v1/jwt/create/
```

Обновление токена:

```
POST /api/v1/jwt/refresh/
```

Проверка токена:

```
POST /api/v1/jwt/verify/
```

---

## 📡 Основные эндпоинты

| Метод                    | Эндпоинт                                 | Описание                                 |
| :----------------------- | :--------------------------------------- | :--------------------------------------- |
| **GET**                  | `/api/v1/posts/`                         | Получить список всех постов              |
| **POST**                 | `/api/v1/posts/`                         | Создать новый пост                       |
| **GET**                  | `/api/v1/posts/{id}/`                    | Получить пост по ID                      |
| **PUT/PATCH/DELETE**     | `/api/v1/posts/{id}/`                    | Изменить или удалить пост                |
| **GET/POST**             | `/api/v1/posts/{post_id}/comments/`      | Получить или создать комментарий         |
| **GET/PUT/PATCH/DELETE** | `/api/v1/posts/{post_id}/comments/{id}/` | Работа с конкретным комментарием         |
| **GET**                  | `/api/v1/groups/`                        | Получить список групп                    |
| **GET**                  | `/api/v1/groups/{id}/`                   | Получить информацию о группе             |
| **GET/POST**             | `/api/v1/follow/`                        | Получить список подписок или подписаться |
| **GET**                  | `/api/v1/follow/?search=username`        | Поиск подписок по имени пользователя     |

---

## 🧠 Модели данных

### **Post**

* `text` — текст поста
* `pub_date` — дата публикации
* `author` — автор поста
* `image` — изображение (опционально)
* `group` — группа (опционально)

### **Comment**

* `author` — автор комментария
* `post` — пост, к которому относится
* `text` — текст комментария
* `created` — дата создания

### **Group**

* `title` — название группы
* `slug` — уникальный слаг
* `description` — описание

### **Follow**

* `user` — кто подписался
* `following` — на кого подписан

---

## 🧾 Пример запроса

```http
POST /api/v1/posts/
Authorization: Bearer <your_token>

{
  "text": "Мой первый пост!",
  "group": 1
}
```

**Ответ:**

```json
{
  "id": 5,
  "author": "user123",
  "text": "Мой первый пост!",
  "pub_date": "2025-10-28T13:45:00Z",
  "group": 1
}
```

---

## 🧩 Авторизация и права доступа

| Действие                           | Доступ                |
| ---------------------------------- | --------------------- |
| Просмотр постов/комментариев       | Все пользователи      |
| Создание поста/комментария         | Только авторизованные |
| Изменение/удаление своего контента | Только автор          |
| Подписки                           | Только авторизованные |

---


## 🧱 Лицензия

Проект распространяется под лицензией **MIT License**.
